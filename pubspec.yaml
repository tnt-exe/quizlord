name: quizlord
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  rflutter_alert: ^2.0.7
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons:

flutter_launcher_icons:
  android: "ic_launcher"
  ios: false
  image_path: "assets/quizlord.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/quizlord.png"
  adaptive_icon_foreground_inset: 30
  web:
    generate: true
    image_path: "assets/quizlord.png"
    background_color: "#FFFFFF"
    theme_color: "#FFFFFF"

flutter:
  uses-material-design: true
